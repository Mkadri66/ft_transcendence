
services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    #volumes:
    #  - ./frontend:/app
    environment:
      - NODE_ENV=development

  backend:
    build: ./backend
    ports:
      - "3000:3000"
    #volumes:
    #  - ./backend:/app
    environment:
      - NODE_ENV=development
